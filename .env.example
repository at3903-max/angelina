# Required: OpenAI API key for the AI testing agent
OPENAI_API_KEY=sk-your-key-here

# Optional: model for reasoning (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# Optional: run browser headless (default: true)
HEADLESS=true
